{
  "name": "proxy",
  "version": "1.0.0",
  "description": "HLS .m3u8 + .ts proxy for Cloudflare Pages + Workers",
  "main": "_worker.js",
  "scripts": {
    "start": "wrangler pages dev",
    "deploy": "wrangler pages publish . --project-name=proxy"
  },
  "dependencies": {
    "hls.js": "^1.5.0"
  },
  "devDependencies": {},
  "type": "module"
}
